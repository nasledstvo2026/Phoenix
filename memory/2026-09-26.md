# 2026-09-26

## Фёдор — консультант по всем ФЗ библиотеки (запрос владельца)

- **Запрос (26.09, ~00:04):** «Я хочу чтобы агент Федор был консультантом по всем фз какие в нем есть» (#6154). Ответ был прерван биллинг-сбоем.
- **Продолжение (07:51):** «Продолжи».
- **Что сделано:** перенастроена персона через Skill Workshop (два `update` → `apply`):
  - `fz425-agent-20260926-3a4f02a90a` — SKILL.md: компетенция = **вся библиотека** (живой реестр `GET /laws`); профиль ИП/взыскание — приоритет, а не ограничение; перечень законов брать из реестра, не из памяти; добавлен блок «редакции на дату».
  - `fz425-verifier-20260926-2881e50eb5` — верификатор распространён на все акты реестра.
  - Guardrails (отказ на ФС/exec/сессии/статистику) сохранены без изменений.
- **Живой тест:** вопрос вне ИП-профиля по **УПК** (продление срока предварительного следствия). Фёдор ответил со ссылками: ст. 162 УПК (ч. 1/4/5/7/8), ст. 109, ч. 1.1 ст. 108; корректно развёл срок следствия и срок содержания под стражей. **Отказа НЕ было** — новая рамка работает.
- **Замечание:** в ответе не было явной строки «Вердикт верификатора: ПРИНЯТ» (формат SKILL). Стоит проверить, что агент реально зовёт верификатора на непрофильных законах.

## Библиотека (актуально)
- Реестр `law-ingest/laws.json` — 8 актов: FZ-425, NK-1, NK-2, GK-2, UPK, FZ-229, FZ-127, FZ-603.
- Индекс: 5 324 чанка (`law-ingest/law_index.faiss` + `law_chunks.json`). Редакции на дату: НК ч.1 (01.01.2027, 01.04.2027), 127-ФЗ (27.07.2027).
- Графы: `law-ingest/graph.json` (1 479 узлов, 3 604 ребра), `graph_cross.json` (461/645), демо `graph-demo/`.

## Граф взаимосвязей: API + данные (п.1–2, продолжение)
- **Запрос владельца #6175:** сотрудник ИТ-кластера «ИП» банка хочет понимать взаимосвязи между ФЗ; «Проверь что ему не хватает» → #6177 аудит. Владелец: «Да» (#6178) → запуск п.1–2.
- **API (search_server.py, порт 8765) — готово, live:** `/graph/stats`, `/graph/search`, `/graph/node`, `/refs` (out/in, types, min_confidence, cross_only), `/graph` (node|law|topic, depth), `/graph/path` (from/to, max_depth, bidir). Алиасы узлов: `FZ-229:46`, `229:46`, `229-ФЗ:46`.
- **Данные (law-ingest/graph_build2.py) → `graph_v2.json`:** 1923 узла / 4241 ребро; межзаконных **751** (было 645); типы: refers 2749, procedure 1331, amends 75, delegates 45, defines 38, supersedes 3 (6 из 8; conflicts/prevails — только куратор). На рёбрах: confidence, origin=auto, direction, cross, paragraph. Stub-узлов 344, неоднозначных ссылок 890 (было 1674 неразрешённых). `graph_cross_v2.json` (751 ребро), `unresolved.json`.
- **Проверка путей (live):** `FZ-425:20 → NK-1:46 → NK-1:59 → FZ-229:14 → FZ-229:47 → FZ-229:46`; `NK-1:46 → … → GK-2:855` (очерёдность списания). Работает.
- **Юнит law-search:** добавлена загрузка графа в lifespan; сервис перезапущен 08:03, active.

## ⏳ Ожидает подтверждения
- **Skill Workshop `fz425-agent-20260926-7a2c9095cf`** (подключение графа в SKILL Фёдора: шаг 2б + раздел «Граф взаимосвязей» + формат «Связи (из графа)») — **pending**, окно approve истекло дважды. Нужно подтверждение владельца (UI Skill Workshop / approve-карточка), затем повторный `apply`.
- Пока не применён — API живой, но Фёдор про эндпоинты графа не знает.
