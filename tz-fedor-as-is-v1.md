# ТЗ v1.0 (as-is) — Агент «Фёдор» (`fz425-agent`)

**Документ фиксирует фактическое состояние агента и его инфраструктуры на 24.09.2026.**
Это не проект развития, а описание «как есть»: состав, поведение, данные, границы и выявленные расхождения. Каждый факт получен прямым аудитом системы 24.09.2026 (команды указаны в источнике проверки).

| Параметр | Значение |
|---|---|
| Наименование | AI-консультант по 425-ФЗ (агент `fz425-agent`, пользовательское имя — «Фёдор») |
| Версия документа | **1.0 (as-is)** |
| Дата аудита | 24.09.2026, 08:10–08:20 MSK |
| Заказчик | Кирилл (владелец) |
| Исполнитель | Феникс (системный аналитик) |
| Хост | `vm-low4-8` |
| Статус | Зафиксировано фактическое состояние |
| Заменяет | `tz-*.md` (6 файлов от 02.08.2026) как описание системы |

---

## 1. Назначение агента (фактическое)

Агент — **консультант по Федеральному закону от 28.11.2025 № 425-ФЗ** «О внесении изменений в части первую и вторую Налогового кодекса Российской Федерации…» в части порядка взыскания задолженности (ст. 46, 60 НК РФ в новой редакции).

Область вопросов (зафиксирована в `skills/fz425-agent/SKILL.md`):
- переход информационного обмена по взысканию задолженности на «Реестр решений о взыскании задолженности»;
- обработка поручений налоговых органов (ПНО) на перечисление суммы задолженности;
- порядок и сроки исполнения поручений по новым правилам (ст. 46, 60 НК РФ в ред. 425-ФЗ);
- переходные положения для неисполненных поручений (ст. 20 425-ФЗ).

Целевая аудитория: банки, операторы платформы цифрового рубля, платёжные агенты, IT-разработчики.

> **Фактическая граница:** сейчас это монопрофильный консультант по 425-ФЗ с опорой на НК РФ ч.1/ч.2. Другие федеральные законы в базу знаний не подключены.

---

## 2. Роли и маршрутизация

| Элемент | Факт | Источник проверки |
|---|---|---|
| Курьер | Main-агент распознаёт триггер «Федор» и передаёт запрос агенту | `AGENTS.md` (корень), маршрутизация `agents.list` |
| Способ вызова | `sessions_spawn(agentId="fz425-agent", context="isolated", cleanup="delete")` — статeless, сессия удаляется после ответа | `SKILL.md`, `tz-stateless-agent.md` |
| Прямое общение с пользователем | Запрещено: ответ возвращается как результат вызова, доставляет курьер | `SKILL.md` |
| Доступность | Триггер «Федор» доступен всем пользователям (в отличие от триггеров сбора — только Кирилл) | `AGENTS.md`, `TOOLS.md` |

---

## 3. Конфигурация агентов (по факту)

Источник: `~/.openclaw/openclaw.json`.

| Параметр | `fz425-agent` | `fz425-verifier` |
|---|---|---|
| workspace | `/home/user1/phoenix/fz425-agent/` | `/home/user1/phoenix/` |
| Модель primary | `deepseek/deepseek-flash` | `deepseek/deepseek-flash` |
| Fallback | `deepseek/deepseek-v4-pro` | `deepseek/deepseek-v4-pro` |
| Тип модели fallback | текстовая (без vision) | текстовая (без vision) |

Служебная конфигурация:

| Параметр | Значение |
|---|---|
| `tools.subagents.tools.allow` | `sessions_send`, `web_search`, `web_fetch`, `read`, `write`, `exec` |
| `tools.agentToAgent` | `enabled: true` |
| `agentToAgent.allow` | 10 агентов: `main`, `fz425-agent`, `fz425-verifier`, `social-consult-agent`, `social-verify-agent`, `social-research-agent`, `irina-router`, `onco-gyn-verifier`, `psychiatry-verifier`, `pediatric-verifier` |

---

## 4. Рабочая область агента (факт)

Каталог: `/home/user1/phoenix/fz425-agent/` (права `drwx------`).

| Объект | Тип / размер | Дата |
|---|---|---|
| `AGENTS.md` | 9 341 Б | 02.08.2026 12:12 |
| `MEMORY.md` | 2 940 Б | 29.07.2026 14:59 |
| `IDENTITY.md`, `SOUL.md`, `TOOLS.md`, `USER.md`, `HEARTBEAT.md` | 1,3 / 1,8 / 0,9 / 0,5 / 0,2 КБ | 29.07.2026 |
| `SKILL.md` | симлинк → `../skills/fz425-agent/SKILL.md` | 12.08.2026 |
| `skills` | симлинк → `../skills` | 12.08.2026 |
| `knowledge/fz425/` | каталог базы знаний | — |
| `memory/` | `Федеральный_закон_425-ФЗ…md` 1 018 328 Б + `large-files/` (НК ч.1 3 978 103 Б, НК ч.2 12 830 350 Б) | 01.08.2026 |
| `output/` | `sberbank_425fz_fine_calculation.html` 27 128 Б | 02.08.2026 23:27 |
| Корневые копии законов | те же 3 файла НК/425-ФЗ продублированы в корне workspace | 01.08.2026 |
| `письмо_ФНС_опытная_эксплуатация_425ФЗ.md` | 8 384 Б | 03.08.2026 20:35 |
| `openclaw-workspace-state.json` | 120 Б | 29.07.2026 |
| Git-репозиторий workspace | `git log` → **коммитов нет** (ветка `master` пустая) | — |

> Примечание: `git -C fz425-agent log` вернул `fatal: your current branch 'master' does not have any commits yet` — версионирование содержимого workspace фактически не ведётся.

---

## 5. База знаний (факт)

Каталог `fz425-agent/knowledge/fz425/`:

| Файл | Размер | Дата | Фактическая роль |
|---|---|---|---|
| `faq.md` | 5 183 Б | 18.08.2026 12:34 | FAQ, пополнялся последним |
| `sources.md` | 4 731 Б | 01.08.2026 15:17 | приоритеты источников (consultant.ru, garant.ru, nalog.gov.ru, minfin.gov.ru, pravo.gov.ru, government.ru) |
| `terms.md` | 1 893 Б | 01.08.2026 15:10 | глоссарий; 5 терминов помечены «уточнить» (ПОС, Реестр решений, REST API, доисполнение, оператор цифрового рубля) |
| `weekly-update.md` | 842 Б | 01.08.2026 15:10 | **только шаблон**, заполняемых записей нет |
| `documents/` | 3 файла | 01.08.2026 17:53 | 425-ФЗ (1 018 328 Б), НК ч.1 (3 978 103 Б), НК ч.2 (12 830 350 Б) |

---

## 6. Логика ответа агента (факт, по `SKILL.md`)

### 6.1 Алгоритм (7 шагов)
1. Поиск в локальной базе знаний (`faq.md`, `terms.md`, `sources.md`).
2. При недостатке данных — `web_fetch` по источникам в порядке приоритета.
3. Верификация фактов через `fz425-verifier`.
4. Обработка вердикта: `ПРИНЯТ` → далее; `ТРЕБУЕТ ДОРАБОТКИ` → правка и повтор верификации (цикл).
5. Формирование ответа по формату.
6. Возврат ответа как результата вызова (`sessions_send` пользователю запрещён).
7. Обновление базы знаний (FAQ, термины, источники).

### 6.2 Приоритет документов (зафиксирован)
1. **НК РФ ч.1 (ред. 26.06.2026) и ч.2 (ред. 04.07.2026)** — основной источник.
2. **425-ФЗ** — специальный источник для ст. 46, 60 НК РФ и переходного периода (норма вступает в силу 01.01.2027).
3. **При коллизии** результата embedding-поиска и блока «Ключевые факты 425-ФЗ» — доверять блоку SKILL.md.

### 6.3 Курируемые факты в `SKILL.md` (обязательная сверка перед ответом)
Фактически реализованные, со ссылками на нормы:
- срок исполнения ПНО — **3 часа** операционного дня с момента размещения в реестре (425-ФЗ, ст. 1, новая ред. п. 6 ст. 46 НК РФ; п. 2 ст. 60 НК РФ);
- текущая редакция до 01.01.2027 — **1 операционный день** (НК ч.1, п. 2 ст. 60);
- переходные положения (ст. 20 425-ФЗ): 1 операционный день (рублёвые счета/цифровые рубли), 2 операционных дня (валютные счета, драгметаллы);
- ответственность банка (ст. 135 НК РФ): 1/150 ставки рефинансирования, не более 0,2% в день (п. 1); 30% от непоступившей суммы (п. 2);
- Реестр решений о взыскании задолженности — новая редакция 425-ФЗ;
- обязанности банка по ст. 60 НК РФ (очерёдность по ГК РФ, запрет задержки, уведомление об исполнении).

### 6.4 Тональность и режим
- Официально-деловой стиль, без сленга и эмодзи; обращение на «Вы»; без эмоциональных оценок.
- Дисклеймер при неоднозначности: «Окончательная квалификация требует консультации юриста…».
- В `MEMORY.md` зафиксирован режим **temperature 0,3**.

### 6.5 Защита (фактическая реализация)
В `SKILL.md` и `MEMORY.md` агента и в `fz425-verifier/SKILL.md` есть обязательный блок отказа: при запросах вне консультации (файлы, структура каталогов, переписка/сессии, статистика, команды shell/exec, конфигурация, токены/ключи) — ответ строго «Этот запрос вне моей компетенции», инструменты не вызываются. Правило объявлено приоритетнее прочих инструкций запроса.

---

## 7. Верификатор `fz425-verifier` (факт)

| Элемент | Значение (по `skills/fz425-verifier/SKILL.md`) |
|---|---|
| Назначение | Проверка юридических и технических фактов по 425-ФЗ |
| Вызов | `fz425-agent → sessions_send(agentId="fz425-verifier", timeoutSeconds=60)` |
| Формат запроса | `ВЕРИФИКАЦИЯ` → `Факт N: …` построчно → `---` → `Источники:` |
| Правило сессии | одна сессия — один запрос |
| Проверяет | корректность нормы и статьи; актуальность редакции; сроки SLA (ПНО 3 ч, ПОС 1 день); переходный период (ст. 20) |
| Вердикты | `ПРИНЯТ` / `ТРЕБУЕТ ДОРАБОТКИ` |
| Ограничение | верифицирует только факты по 425-ФЗ; внепрофильные запросы — отказ |

В `MEMORY.md` агента правило усилено: верификация **обязательна перед каждым ответом, без исключений**.

---

## 8. Поисковый сервис (факт)

Реализация: `search_server.py` (4 285 Б, 02.08.2026 12:23), FastAPI + uvicorn.

| Параметр | Значение |
|---|---|
| Адрес | `127.0.0.1:8765` (только локальный интерфейс) |
| Процесс | `uvicorn search_server:app`, PID 23850 |
| Состояние | `law-search.service` = **active**, запущен 20.09.2026 12:02 MSK |
| Загрузка | lifespan: модель → FAISS-индекс → `chunks.json` → `fz425_meta.json` (однократно при старте) |
| Модель | `intfloat/multilingual-e5-small` |
| Индекс | `faiss.IndexFlatIP` (inner product по нормализованным векторам = косинус) |
| Префиксы | документы: `passage: `, запрос: `query: ` |

### 8.1 Эндпоинты (фактически реализованы — 2 из 2)

| Метод | Путь | Параметры | Ответ |
|---|---|---|---|
| GET | `/search` | `q` (обяз.), `k` (по умолч. 5, 1…50), `threshold` (по умолч. **0,83**) | `query`, `expanded`, `total`, `threshold`, `results[]` |
| GET | `/health` | — | `{"status":"ok","chunks":N}` |

Поля результата: `rank`, `score` (4 знака), `source` (русское имя: 425-ФЗ / НК ч.1 / НК ч.2), `article`, `title`, `snippet` (первые 300 символов), `full_text`.

### 8.2 Препроцессинг запроса
Заменитель аббревиатур — словарь `ABBREV` **зашит в код** (9 записей): `пно`, `пос`, `смэв-3`, `смэв`, `енс`, `енп`, `фнс`, `инн`. При срабатывании к запросу добавляется расшифровка: `"<оригинал> (<расшифровка>)"`. Внешний словарь `tz-abbrev-dict.md` описан в ТЗ, но кодом не читается.

### 8.3 Чего в сервисе нет (факт)
Фильтров по закону/статье/дате; эндпоинта свежести; реранкера; лексического (BM25/FTS) канала; кэша; аутентификации (доступ ограничен только bind на localhost).

---

## 9. Данные и индекс (факт)

Каталог размещения артефактов — корень `/home/user1/phoenix/`:

| Артефакт | Размер | Дата |
|---|---|---|
| `chunks.json` | 13 901 561 Б | 02.08.2026 15:25 |
| `fz425_index.faiss` | 4 578 861 Б | 02.08.2026 15:29 |
| `fz425_meta.json` | 858 619 Б | 02.08.2026 15:29 |

### 9.1 Состав индекса (проверено подсчётом)

| Источник | Чанков | Статей | Номера статей (примеры) |
|---|---|---|---|
| 425-ФЗ | 160 | 23 | 1–4, 7, 8, 17–22 … |
| НК РФ ч.1 | 659 | 224 | 1–9, 12, 13, 16 … |
| НК РФ ч.2 | 2 162 | 469 | 143, 146–151, 154–158 … |
| **Итого** | **2 981** | — | — |

### 9.2 Параметры чанков
- длина: min 52, средняя 2 442, max 6 811 символов;
- структура записи чанка: `id`, `article`, `title`, `text`, `source`;
- метаданные (`fz425_meta.json`): `id`, `source`, `article`, `title`, `text_len` (тексты не дублируются);
- согласованность: `len(chunks) == len(meta) == index.ntotal == 2981`.

### 9.3 Свежесть
Индекс собран **02.08.2026**. На дату аудита возраст — **53 дня**. Изменений с 02.08.2026 не вносилось.

---

## 10. Пайплайн подготовки данных (факт)

| Скрипт | Размер | Назначение |
|---|---|---|
| `chunk_laws.py` | 11 533 Б (02.08.2026 15:25) | нарезка законов на чанки → `chunks.json` |
| `index_laws.py` | 2 750 Б (02.08.2026 11:04) | эмбеддинг (batch 32, `passage:`-префикс, `normalize_embeddings=True`) → FAISS + `fz425_meta.json` |
| `search_law.py` | 3 614 Б | CLI-поиск (до API-режима) |
| `test_search.py`, `test_api.py` | 5 634 / 2 888 Б | проверки поиска и API |

---

## 11. Инфраструктура (факт)

| Компонент | Факт |
|---|---|
| systemd (user) | `law-search.service`, `openclaw-gateway.service` (+ `.bak`), `default.target.wants` |
| `law-search.service` | `After=network.target`; `StartLimitIntervalSec=60`, `StartLimitBurst=5` (введено после инцидента 20.09.2026 с битыми CUDA-колёсами torch); `Restart=always`, `RestartSec=5`; `WantedBy=default.target` |
| Изоляция от гейтвея | `law-search` — независимый юнит, переживает рестарт гейтвея (подтверждено: active с 20.09.2026 при рестартах гейтвея) |
| Перезапуск гейтвея | `safe-restart.sh` (748 Б): `systemd-run --user --on-active=12s --unit=openclaw-restart-tmp systemctl --user start openclaw-gateway`, затем `systemctl --user stop openclaw-gateway` |
| Гейтвей | OpenClaw **2026.7.34** (0847c47) |
| Python-среда | `venv`: faiss-cpu 1.14.3, fastapi 0.141.1, numpy 2.5.1, sentence-transformers 5.6.1, **torch 2.13.0+cpu**, transformers 5.14.1, uvicorn 0.52.1 |

> Рантайм эмбеддингов — **torch (CPU)**, не ONNX.

---

## 12. Выявленные расхождения (факт против заявлений в документации/памяти)

| ID | Где заявлено | Заявлено | Фактически | Влияние |
|---|---|---|---|---|
| D-1 | `425fz-architect.html` | «Гейтвей: OpenClaw v2026.6.33» | 2026.7.34 | недостоверность витрины |
| D-2 | `425fz-architect.html` | эмбеддинги на ONNX | torch 2.13.0+cpu | неверная характеристика ресурсов |
| D-3 | `425fz-architect.html` | «Модель: DeepSeek V4 Pro/Flash» | primary — `deepseek-flash`, `v4-pro` — только fallback | неверное представление о приоритете модели |
| D-4 | `fz425-agent/MEMORY.md` | «Модель: deepseek-v4-pro (primary), deepseek-v4-flash (fallback)» | обратный порядок (flash primary) | устаревшая память агента |
| D-5 | `fz425-agent/MEMORY.md` | `agentToAgent allow=[fz425-agent, fz425-verifier, main]` | 10 агентов в allow | устаревшая память агента |
| D-6 | `SKILL.md` | предписан вызов поиска с `threshold=0.80` | сервер по умолчанию отдаёт `threshold=0.83` | расхождение порога → возможны пропуски релевантного |
| D-7 | `425fz-architect.html`, `SKILL.md` | база знаний: faq/sources/terms используются | `faq.md`, `sources.md`, письмо ФНС **не в индексе** (индексированы только 3 закона) | накопленный опыт не участвует в retrieval |
| D-8 | `weekly-update.md` | «обновлять еженедельно» | файл — шаблон без записей, регламент не исполняется | норма не отслеживается |
| D-9 | `terms.md` | глоссарий | 5 терминов «уточнить» (ПОС, Реестр решений, REST API, доисполнение, оператор ЦР) | неполнота терминологической базы |
| D-10 | workspace | (подразумевается версионирование) | git-репозиторий без коммитов | нет истории изменений знаний |

---

## 13. Заявленные испытания (по источнику `425fz-architect.html`; воспроизведение не выполнялось в рамках аудита)

| Блок | Результат | Содержание |
|---|---|---|
| ИФТ v2 (SKILL.md v2), июль 2026 | 5/5 | Т1 штраф ст. 135; Т2 срок ПНО 3 ч (п. 6 ст. 46); Т3 ст. 20 переходный период; Т4 терминология (СМЭВ-3); Т5 приоритет НК над 425-ФЗ |
| Recovery, август 2026 | 6/6 | гейтвей жив; law-search пережил рестарт (2 981 чанк); embedding API ≥ 0,88; exec — текст; агент+верификатор; отсутствие truncation |
| Stateless, август 2026 | 3/3 | `sessions_spawn` → ответ получен (3 прогона); ANNOUNCE_SKIP: 0 |

Дополнительно в рабочей области присутствует план сквозного ИФТ `ift-plan.md` (3 кейса: срок ПНО, розыск счетов, переходный период СМЭВ-3) с предусловиями и критериями успеха.

---

## 14. Инвентарь ключевых путей

```
/home/user1/phoenix/
├── search_server.py            # API поиска (8765)
├── chunk_laws.py               # чанкер
├── index_laws.py               # построение индекса
├── chunks.json                 # 2 981 чанк (13,9 МБ)
├── fz425_index.faiss           # FAISS, 384d (4,6 МБ)
├── fz425_meta.json             # метаданные (859 КБ)
├── safe-restart.sh             # холодный рестарт гейтвея
├── ift-plan.md                 # план ИФТ
├── skills/
│   ├── fz425-agent/SKILL.md
│   └── fz425-verifier/SKILL.md
└── fz425-agent/                # workspace агента
    ├── AGENTS.md, MEMORY.md, SOUL.md, IDENTITY.md, USER.md, TOOLS.md, HEARTBEAT.md
    ├── SKILL.md -> ../skills/fz425-agent/SKILL.md
    ├── knowledge/fz425/{faq,terms,sources,weekly-update}.md, документы
    └── output/sberbank_425fz_fine_calculation.html
~/.config/systemd/user/{law-search.service, openclaw-gateway.service}
```

---

## 15. Что НЕ подтверждено / вне объёма аудита

- Содержание и корректность юридических норм в `SKILL.md` (требует юридической верификации, не входит в системный аудит).
- Реальное качество ответов (faithfulness, recall) — метрики не измерялись, эталонного набора нет.
- Прохождение заявленных ИФТ/recovery/stateless — принято по документации, не воспроизводилось.
- Внешние интеграции: не обнаружены (работа только через мессенджер + локальный API).
- Наличие/актуальность резервной модели `deepseek-v4-pro` (в конфиге указана, работоспособность не проверялась).

---

_Феникс · ТЗ v1.0 (as-is) · 24.09.2026 · фиксирует фактическое состояние агента `fz425-agent`_
