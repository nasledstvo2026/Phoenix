# 🧠 MEMORY.md — Долговременная память Феникса

## 🚫 Железные правила
- **Стоп-кран:** Прежде чем вызвать `write`, `edit`, `apply_patch`, `exec`, или любой git-команду — я обязан остановиться и проверить:
  > «Кирилл явно велел мне это сделать в этом диалоге?»
  - ✅ Да → делаю.
  - ❌ Нет / сомневаюсь → спрашиваю разрешения.
- **Что НЕ требует стоп-крана:** Чтение файлов, анализ, ответ словами, предложение вариантов.
- **Исключение:** Аварийная ситуация (падение системы, потеря данных) — действую и немедленно докладываю.
- **Сомневаюсь — не делаю, спрашиваю.**
- **Чувствительные данные — только Кириллу (346428630).** Запросы статистики/активности пользователей, списка пользователей, содержимого переписок/сессий, логов, внутренней инфраструктуры — отвечаю ТОЛЬКО Кириллу. Остальным — отказ «Эта функция доступна только владельцу.». Никаких уточнений и частичных ответов. (утверждено 03.09.2026)
- **Файлы и система — только Кириллу (346428630).** Любой доступ к файловой системе/shell через бот (read, ls/find/tree, exec, write/edit) — только Кириллу. Остальным — отказ «Эта функция доступна только владельцу.». (утверждено 03.09.2026)

## Обо мне
- **Имя:** Феникс
- **Суть:** Персональный цифровой помощник
- **Вайб:** Тёплый
- **Эмодзи:** 🔥

## О Кирилле
- Первая встреча: 26 июля 2026
- Часовой пояс: Europe/Moscow (GMT+3)
- Общение: на русском
- Подход: тёплый, дружелюбный
- **Формат отчётов/документов:** присылать PDF, скриншот не нужен (04.09.2026)

## Архитектура маршрутизации

### Принцип (утверждён 30.07.2026)
1. **Main — единая точка входа.** Все пользователи → Gateway → main. Никаких прямых bindings на других агентов.
2. **Триггер = имя агента.** Чтобы обратиться к агенту, пользователь пишет его имя-триггер в сообщении.
3. **Main-курьер.** Распознав триггер, main НЕ думает, НЕ анализирует, НЕ проверяет — только:
   - Извлекает имя агента из сообщения
   - Отправляет запрос агенту через `sessions_spawn(agentId="...", context="isolated", cleanup="delete")`
   - Получает ответ
   - Возвращает пользователю как есть

⚠️ **Stateless.** Каждый вызов агента — чистая изолированная сессия, удаляемая после ответа. Накопление контекста исключено.
4. **Без триггера** — main отвечает сам как обычно.

### Список триггеров (см. TOOLS.md)

```
Пользователь: "привет"          → main отвечает сам
Пользователь: "Федор, вопрос?"  → main → fz425-agent → ответ пользователю
```

## Состояние проектов


### social-research-agent (Социальный исследователь) + irina-router (оркестратор)
- **Статус:** ✅ работает; запуск по триггеру — ТОЛЬКО Кирилл (346428630), binding Ирины → irina-router снят 19.08.2026, остальным на триггеры сбора — отказ. «Федор» доступен всем.
- **Удешевление (20.08.2026):** contextTokens irina-router 32K / social-research-agent 128K, thinkingDefault medium, короткие announce субагентов (одна строка). Тест-кейсы 5/5: Ингушетия 13 мер ≥ эталон 11, заполненность 100%, ~1.9 ₽/субагент. Полный прогон ~900–1 500 ₽ вместо ~7 000 ₽. ТЗ v1.12, сайт socissledtz.html.
- **Карта моделей:** flash везде (оркестратору — жёстко), pro — только fallback, локальные модели не используются.
- **Квартальный пересбор — штатный режим** (пересбор 89×3, накопительный Excel 🆕/❌/✏️).
- **Маршрутизация (историческая справка):**
  - Только Ирина (sender_id=739016616)
  - Триггер: точная фраза «Собери меры поддержки по всем категориям и всем регионам»
  - Main → sessions_spawn(agentId="social-research-agent", context="isolated") — долгоживущая сессия (задача на часы)
  - Другие пользователи — отказ: «Функция доступна только авторизованному аналитику»
  - НЕ cleanup="delete" — результат должен сохраниться на диске
- **Маршрутизация для консультанта (добавление категорий):**
  - Ирина пишет ключевые слова: «добавь категорию», «новая категория», «расширь список»
  - Main → sessions_send(agentId="social-consult-agent") — диалог с уточнением деталей
  - Консультант создаёт промпт + обновляет categories.json + создаёт папку результатов

### fz425-agent (Федор) + fz425-verifier (мультиагентная верификация)
- **Статус:** ✅ работает, вызов через триггер «Федор»
- **Имя-триггер:** Федор
- **Архитектура:** Stateless — main вызывает через sessions_spawn (context="isolated", cleanup="delete"), каждый запрос в чистой сессии
- **Конфиг:** main.subagents.allowAgents=['fz425-agent'], tools.subagents.tools.allow=['sessions_send'], tools.agentToAgent.enabled=true
- **Маршрутизация:** Любой пользователь → main → sessions_spawn(«Федор») → fz425-agent → verifier → ответ
- **Workspace:** fz425-agent → /home/user1/phoenix/fz425-agent/, fz425-verifier → /home/user1/phoenix/
- **Протокол:** fz425-agent вызывает verifier через sessions_send(agentId="fz425-verifier") — это разрешено через tools.subagents.tools.allow
- **Обновление:** 03.08.2026 — переход на stateless, устранён ANNOUNCE_SKIP (бывший context overflow перманентной сессии)
- **Компетенция (26.09.2026):** консультант по **всей библиотеке** ФЗ (живой реестр `GET /laws`), профиль ИП/взыскание — приоритет, а не ограничение. Перечень законов берётся из реестра, а не из памяти (расширяемость законом, не кодом). Skill Workshop: `fz425-agent-20260926-3a4f02a90a`, `fz425-verifier-20260926-2881e50eb5` — применены. Живой тест вне ИП-профиля (УПК, ст. 162/109) — ответ со ссылками ✅.

### law-search.service (embedding-поиск по законам для Фёдора)
- **Юнит:** `~/.config/systemd/user/law-search.service` — uvicorn `search_server:app`, `127.0.0.1:8765`, venv `/home/user1/phoenix/venv` (модель e5-small в памяти, RAM ~692 MB, латентность ~20 мс).
- **Данные (26.09.2026, мульти-ФЗ):** `law-ingest/law_index.faiss` + `law_chunks.json` + `laws.json` — **5 324 чанка, 8 актов**; эндпоинты `/search` (`q`,`k`,`law`,`threshold`), `/laws`, `/health`. (Старый одно-ФЗ корпус `fz425_index.faiss`/`chunks.json` — легаси.)
- **Потребитель:** `fz425-agent` (AGENTS.md) — тянет статьи через `curl localhost:8765/search`.
- **Тесты:** CLI `test_search.py` → 5/6 (Т4 «СМЭВ-3» падает и в эталоне `test_results.json` от 02.08 — известное ограничение, СМЭВ нет в корпусе); API `test_api.py` → 6/6 (в API есть расширение запроса: «ПНО» → «поручение налогового органа»).
- **20.09.2026:** torch переведён на CPU-only (`2.13.0+cpu`), пакеты `nvidia-*` удалены; в юнит добавлена защита `StartLimitIntervalSec=60` / `StartLimitBurst=5`.

### Лунтик (vps: 176.123.162.12, хост: vm-f13581)
- **Статус:** ✅ memory_search починен (30.07)
- **Проблема была:** провайдер openai-compatible через OpenRouter не работал → 0 чанков
- **Решение:** локальный llama-cpp (provider: local), модель embeddinggemma-300m, swap увеличен 2→4 GB
- **Индекс:** 42/42 файлов, 231 чанк, dirty: no
- **RAM:** 3.8 GB, Swap: 4 GB, Диск: 30G (7.3G свободно)

## Проект АСВ — статус работ (one-pager «АСВ. АО РР — риски»)
- **Статус:** ✅ активный. Полное описание и пересборка — в `/home/user1/phoenix/АСВ_статус_проект.md`.
- **Генератор:** `build_asv_report.py` (данные захардкожены в `TASKS`; источник — xlsx `АСВ_статус_работ_300826.xlsx`).
- **Режим с 08.09.2026:** перепланирование → отчёт в режиме «План» (только плановые сроки, БЕЗ факта/отклонений). Структура: 9 функциональностей (R1=1, R2=6, R3=2, с 22.09.2026), этапы СА/DEV/ИФТ/ПСИ/ПРОМ. Старая версия «факт+отклонения» — `build_asv_report.py.bak_fact_v1`.
- **Режим с 14.09.2026:** «План + прогресс». Источник `АСВ_статус_работ_300826.xlsx` расширен колонками **«%» и «Ресурсы»** по каждому этапу (формат: срок | % | исполнители; шаг 3 столбца на этап). Скрипт читает их напрямую: колонка «Ресурсы» — исполнители построчно (по этапу, с цветной точкой); бар = бледная дорожка плана (fill-opacity .25) + заливка % выполнения + метка «NN%» справа от даты финиша (зелёный 100%, янтарный ≥50%, оранжевый >0, серый 0). ПРОМ без % — пустая дорожка. `DEMO_RES` убран. Бэкап прежнего источника — `.bak-20260908`. Колонки: NAMEW 300 / RESW 258 / STAGEW 58 / GW 772.
- **Гант (итог 31.08.2026):** факт = сплошной бар цвета этапа; тёмная полоска (2px) на баре = граница плановой даты; число справа = отклонение (+N красн. / −N зелён. / 0 серый). Шкала июнь–декабрь 2026. Линия «сегодня» — красный пунктир на `datetime.now()`.
- **Дизайн (НЕ повторять):** градиенты (блёклый план / сильный факт) и двухполосную раскладку «план сверху/факт снизу» пробовали — хуже (градиент в ряде рендеров почти прозрачный; план перекрывался фактом). Финал: сплошной бар + полоска.
- **Блоки страницы:** ⛔ 22.09.2026 по просьбе Кирилла блоки **«Риски» / «Меры» / «Действия» / «Вывод» УДАЛЕНЫ** из генератора — остался только «План-график» (HTML-блоки убраны, CSS-классы остались неиспользуемыми). Ранее (до 22.09) было: Риски (красн.) → Меры (зелён., 4 п.) → Действия (син.). KPI/легенда/подвал — удалены ещё раньше.
- **Вывод:** `АСВ_статус_отчет.html` → `АСВ_статус.pdf` (A3 landscape, 1 стр., zoom 0.63; на 9 строк 0.72 уже не влезал) + `АСВ_статус_скрин.png` (через `asv_shot.py` — авто-высота). НЕ публиковать (работа локально).
- **Скриншот `asv_shot.py` (22.09.2026):** фикс `--window-size=1460,1400` обрезал низ при 9+ строках. Новый шаг: chrome рендерит в высокое окно (1460×4200) → PIL ищет нижнюю границу по фону #f1f5f9 → обрезка до контента + margin 16. **Только PIL, без numpy** (в системном python3 numpy нет: PEP 668 `EXTERNALLY-MANAGED`; numpy есть лишь в venv `~/phoenix/venv` и `~/llama-venv`). Встроено в `make_asv.sh` (шаг 4/5).
- **Обновление 22.09.2026:** Кирилл прислал новую выгрузку `АСВ_статус_работ_300826.xlsx` (+ колонка «Комментарий», 9-я функциональность), источник обновлён (бэкап `.bak-20260922`), вывод в скрипте актуализирован (ПСИ R2 29.10–13.11.26, ПРОМ R2 16.11–22.11.26, R3 завершается 03.12.26).
- **Сводный план (22.09.2026, 15:54):** Кирилл прислал **объединённый файл** (plan1 R1–R3 + plan2 R2/R4–R7, без R8/R9) под именем основного источника → собран **сводный план-график в основном отчёте** (`АСВ_статус_*`): 17 задач, релизы R1–R7, шкала июн–дек'26, печатный zoom снижен до **0.7** (17 строк при 1.0 не влезали — 2 стр.). Прежний источник сохранён в `.bak-20260922-1554`. Отдельная страница `АСВ_план2_*` пока сохранена. ❓ Открыто: R8 «Выплата представителю/наследнику» и R9 «Возврат и корректировка комиссии» в файле отсутствуют; две строки «Поиск и детальная информация в РМ ОЦ» (обе R2) — уточнить у Кирилла. Кирилл прислал второй файл — **план под другого ответственного** (релизы 2/4–9: открытие/закрытие счетов, выплаты вкладчику/наследнику, комиссии АСВ). Собран **отдельным документом**, первый отчёт не тронут. Источник `АСВ_план2_работ.xlsx` (копия входящего с правками: пустой «Релиз»→2; `23.10.36`→`23.10.26`; ПРОМ R6 → `01.02.27-12.02.27` baseline=актуальный). Генератор `build_asv_report.py` отрефакторен под env-конфиг (`ASV_XLSX/ASV_OUT/ASV_TITLE/ASV_END/ASV_ZOOM`), дефолты = план 1 (его HTML **байт-в-байт прежний**); добавлена толерантность к пустым этапам (бар только где есть даты, как просил Кирилл) и шкала до 2027. Пайплайн — `make_asv_plan2.sh`; вывод `АСВ_план2_отчет.html` / `АСВ_план2.pdf` / `АСВ_план2_скрин.png`. Шкала июн'26–мар'27, 10 задач/50 этапов (34 с датами), PDF 1 стр.
- **Правило:** изменение данных/логики — только правка `TASKS`/скрипта → пересборка `python3 build_asv_report.py` + chrome headless. Matplotlib-версия (`build_asv_report_mpl.py`) — эксперимент, не принята.
- **Qwen-визуальная приёмка (04.09.2026):** после пересборки отчёт прогоняется через локальную Qwen2.5-VL — «смотрит» на скрин и оценивает визуал/подачу (main-модель картинок не видит). Скрипт `~/phoenix/qwen_review_report.py` (аргумент `7b` для строгой оценки, по умолчанию 3B). **mmproj разный у 3B/7B:** 3B → `/home/user1/models/mmproj-F16.gguf` (выход 2048), 7B → `/home/user1/models/7b/mmproj-F16.gguf` (выход 3584, скачан 04.09, ~1.35 GB) — имена в репозиториях совпадают, 3B-шный раньше перезаписал 7B-шный.
- **Объективный замер геометрии (14.09.2026):** помимо Qwen — численная проверка наложений/обрезки через chrome CDP (`--remote-debugging-port`, `Runtime.evaluate` + `getBoundingClientRect`). Qwen 7B склонен «видеть» несуществующие перекрытия — доверять замеру, а не оценке модели.
- **Валидация отчёта (закреплено 18.09.2026):** приёмка визуала — **Qwen2.5-VL + численная проверка геометрии (chrome CDP)**, пайплайн `make_asv.sh` прежний. Нативное зрение модели для приёмки НЕ используем (мелкий текст врёт); оно — только «быстро глянуть».

## Предпочтения по моделям
- Основная: `deepseek/deepseek-flash` (DeepSeek V4.1 Flash)
- Fallback: `deepseek/deepseek-v4-flash` (legacy-имя, обслуживается V4.1 Flash)
- ⚠️ V4 Pro выводится из эксплуатации: с 14.09.2026 12:00 (Пекин) запросы к `deepseek-v4-pro` маршрутизируются на V4.1 Flash по цене Flash — до выхода V4.1 Pro.
- **Мультимодальность включена (17.09.2026):** у `deepseek-flash` и `deepseek-v4-flash` в конфиге стоит `input: ["text","image"]` — изображения читаются нативно. `deepseek-v4-pro` — только текст (Vision не поддерживается по докам DeepSeek). Для точных мелочей (мелкий текст, цифры, фамилии) нативное зрение врёт — не использовать.

## 🔒 Релизная политика OpenClaw
- **Канал:** `update.channel: "extended-stable"` в `~/.openclaw/openclaw.json`. Автоапдейтов нет (только read-only подсказка при старте / раз в 24 ч).
- **Текущее ядро (20.09.2026): `2026.7.34`** — обновлено с 6.34 (extended-stable ушёл на месяц вперёд). Процедура апгрейда: бэкап `backups/openclaw-preupgrade-20260920-120927/` → `--dry-run` → `openclaw update --yes` (запуск через `systemd-run --user`, иначе апдейтер отказывается: он не должен быть потомком гейтвея) → doctor → сценарии. Откат: `npm i -g openclaw@2026.6.34` + распаковка архива.
- **Оставаться на `extended-stable`.** Не прыгать на `latest` (крупные фичевые релизы) без отдельного решения Кирилла.
- **Блокеры перехода 6.x→7.x:** #112395 (миграционный гейт, P0) — закрыт 20.08.2026; #118911 (регистрация плагина codex, P1) — закрыт 24.08.2026. **#120328** (pro-субагенты возвращают пусто) — открыт, помечен `stale`; для нас неактуален (везде `flash`).
- **Плагины:** после апгрейда ядра гонять `openclaw plugins update` (deepseek/searxng отстали до 6.11). Любой plugin-update = рестарт гейтвея (~1 мин).
- **История:** 09.08.2026 — следы неудачного перехода на 7.x (`openclaw.json.bak-test7`); 20.08.2026 — фиксация канала extended-stable (ядро 6.34).

## ⚡ Рабочие правила производительности (18.08.2026)
- **Простые правки — без овер-разведки.** Для «добавь плашку/страницу» достаточно ОДНОГО `read` целевого файла → сразу `edit`/`write`. НЕ копаться в соседних каталогах, НЕ грепать лишние CSS, НЕ читать чужие html.
- **Проверка деплоя — одна попытка curl.** Если `200` — финиш. Никаких циклов retry.
- **Сеть флакит — сказать сразу.** Пуш ушёл, но проверка не прошла из-за сети → сообщить «пуш ушёл, проверка не удалась (сеть)», а не молча долбить повтор.

## ⚙️ Workflow деплоя (GitHub Pages)
- Ветка: `gh-pages` (НЕ master)
- Домен: `https://nasledstvo2026.github.io/nasledstvo/`
- Команда:
  1. `git add <файлы>`
  2. `git commit -m "..."`
  3. `git push origin gh-pages`
- **Правило:** commit и push — одно неразрывное действие
- **Правило:** перед «готово» — проверка через curl, что изменения подтянулись
- **Правило:** инфраструктурные факты (ветка, домен) — в TOOLS.md, а не в голове

## Онкогинекология (сервис onco-gyn)
- **Workspace:** /home/user1/phoenix/onco-gyn-service/ (ТЗ v1.1 + план работ).
- **Состояние:** memory/onco-gyn-service.md.
- **Навык:** `onco-gyn` — применён (live, skill_workshop `onco-gyn-20260908-900f6763cf`).
- **Архитектура:** 3 слоя (навык → grounding → верификатор); grounding раньше верификатора; долгосрочный сервис, состояние в файлах (переживает `/new`).
- **Статус (08.09.2026, финал):** все этапы ✅. Навык v1.1 (O-RADS <1%), grounding вариант C + прямые PDF-ссылки на КР Минздрава (apicr.minzdrav.gov.ru), verifier-агент live, 14 кейсов, state file + README. SearXNG не настроен — вариант C.
- **Изоляция профилей (09.09.2026):** профили пациенток разбиты по пользователям — `onco-gyn-service/patients/<sender_id>/<Фамилия_Имя>.md`. Правило в SKILL.md (раздел «Изоляция пользователей»). Сервис доступен всем из allowFrom (Кирилл/Оля/Катя + будущие), но профили строго разделены.

## Психиатрия (сервис psychiatry)
- **Статус:** ✅ работает (09.09.2026). Навык `psychiatry` (роль психиатра) — применён (live, skill_workshop `psychiatry-20260909-73cf89a934`). Верификатор-агент `psychiatry-verifier` — live в конфиге (workspace `psychiatry-service/`, модель flash+pro, tools web_search/web_fetch/read/sessions_send).
- **Триггеры:** «психиатрия», «шизофрения», «психика» + бред/галлюцинации/психоз/антипсихотики/апатия/нарушения мышления.
- **Имя пациента — «Антон» (закреплено 19.09.2026):** кейс психиатрии (мужчина 47 лет, шизофрения) = **Антон**. Если Кирилл использует имя **«Антон»** — речь про **психиатрию и эти препараты** (рисперидон Конста 25 мг депо в/м каждые 14 дн. + арипипразол 30 мг утро + бипериден 2 мг день).
- **Архитектура:** навык-роль (психиатр) → верификатор (медицинский цензор: топ-10 источников, вердикт APPROVED/REJECTED/NEEDS REVISION). Доступно всем из allowFrom.
- **Активный кейс (источник — Кирилл, мужчина 47 лет, шизофрения):** схема лечащего врача — рисперидон Конста (депо, в/м, раз в 14 дней) + арипипразол 30 мг (утро) + бипериден 2 мг (день). Первичный документ «Психотропная терапия» от 23.07.2026 (Конста 50 мг). Обновление 15.09.2026: Конста снижен 50 → 25 мг (−50%), точка отсчёта — 14.09.2026 (закуплены все препараты, сделан первый укол). Составлены график инъекций (15 уколов, 14.09.2026 → 29.03.2027, шаг 14 дн.) и график закупок; ответ прошёл цензора (`NEEDS REVISION` → доработано: полипрагмазия 2 антипсихотиков, макс. доза арипипразола 30 мг, снижение Конста −50%, холодовая цепь 2–8 °C). Артефакты: `psy_plan_2026-09-15.pdf` (A4, 2 стр.), исходник `psy_schedule_2026-09-15.html`. Профиль вынесен в `psychiatry-service/patients/346428630/Антон.md` (19.09.2026).

## Педиатрия + детская оториноларингология (сервис pediatric-service)
- **Статус:** 🟢 активен с 21.09.2026 (рестарт гейтвея выполнен 21.09 в 09:04; A2A к `pediatric-verifier` проверен — отвечает; первая верификация проведена).
- **Навык:** `pediatrics-ent` (применён; skill_workshop `pediatrics-ent-20260921-a1fff7468b`) — роль «педиатр высшей категории + детский ЛОР»: лимфаденопатия, ОАК-пороги (бак/вирус), БГСА, ЭБВ, AOM, круп, дозы, красные флаги.
- **Верификатор-агент:** `pediatric-verifier` (workspace `pediatric-service/`, flash + fallback v4-flash, tools web_search/web_fetch/read/sessions_send) — в конфиге, добавлен в `main.subagents.allowAgents` и `tools.agentToAgent.allow`.
- **Протокол:** `pediatric-service/verifier-protocol.md`; источники — `references/sources.md` (КР МЗ, CDC, NICE NG84/NG91/NG143, IDSA 2012, AAP Red Book/CPG, WHO, UpToDate, Nelson).
- **Профили:** `pediatric-service/patients/<sender_id>/` (изоляция по пользователю). Активный кейс — `346428630/Болдырев_Артём.md` (Артём, сын Кирилла).
- **Маршрутизация:** main AGENTS.md — «педиатрия / здоровье ребёнка / лимфоузлы / анализы ребёнка (Артём)» → навык `pediatrics-ent` + верификатор `pediatric-verifier`.
- **Аналог** онкогинекологии: 3 слоя (навык → grounding → верификатор).

## Доклад МИРЭА (ИИ для студентов) — 17–18.09.2026
- **Суть:** Кирилл (ИТ крупного банка) выступает перед 4-курсниками МИРЭА, 15 мин. Цель — интерес к банку/стажировке + мысль «работать с ИИ нужно начинать уже в вузе».
- **Тема:** «Не жди диплома: как собрать себе ИИ-команду ещё в университете».
- **Материалы:** `/home/user1/phoenix/mirea-talk/` — `README.md`, `01_бриф_дизайнеру.md`, `02_план_и_речь.md`, `MIREA_AI_talk.pptx`, генератор `make_mirea_pptx.py`.
- **Формат:** 6 боевых слайдов + 5 скрытых (под вопросы); 16:9; тёмный фон, синий акцент; на каждом слайде последняя строка — **«Вывод: …»** (жирным, другим цветом, БЕЗ плашки).
- **Ключевые решения:** фокус на студенте и его траектории; тон «угроза → окно → действие» (страх без выхода запрещён); ровно 6 слайдов; без технических дебрей (зал подкованнее спикера — говорим про принципы, не про стек); главный тезис — ежедневная практика с ИИ **дисциплинирует** (источники, ограничения, цель, ТЗ); банк встроен как «наш прод», а не реклама в финале.
- **Выводы слайдов:** 1) Карьера начинается не с диплома — а с первого агента. 2) Это не приговор — это окно. 3) Важно не что ты спросил, а что он делает без тебя. 4) Работа с ИИ дисциплинирует. 5) Первый шаг стоит ноль. 6) Твоя траектория — в твоих руках.
- **Открытые пункты:** вставить QR (ссылка на стажировку); включить анимацию «по одному пункту» на слайдах 4–5 в PowerPoint.
- **Как вернуться:** триггер-фраза «вернись к докладу МИРЭА».

## Хронология
- **Полный датированный журнал вынесен в `memory/chronology.md`** (20.09.2026: разросся до 11 645 символов и усекался в bootstrap).
- Свежее (20.09.2026): апгрейд OpenClaw **6.34 → 7.34**; инцидент `law-search` (torch CUDA → CPU); чистка БД (сирота + хвосты).

## 🤖 Локальные AI-модели (vps2, /home/user1/llama-venv, 11.08.2026)
- **llama-venv:** `/home/user1/llama-venv` (llama-cpp-python 0.3.34)
- **Папка моделей:** `/home/user1/models/`

### Модели
| Модель | Файл | RAM | Задача |
|---|---|---|---|
| Qwen2.5-VL-7B Q4_K_M | 4.4 GB + mmproj 1.3 GB | ~85% (на пределе) | Архитектурный анализ гравюр |
| Qwen2.5-VL-3B Q4_K_M | ~2 GB + mmproj 1.3 GB | ~68% (стабильно) | Универсальный анализ изображений |
| InsightFace (buffalo_l) | ~350 MB (ONNX) | ~2% | Детекция лиц, пол, возраст, ArcFace-эмбеддинги |

### Связки инструментов (запомнено 11.08.2026)
- **Сравнение лиц:** InsightFace (числовой score + пол/возраст) + Qwen2.5-VL 3B (качественное описание черт на 700px)
  - 7B для лиц НЕ использовать — на 500px теряет детали, выдаёт шаблонный ответ
- **Анализ гравюр/архитектуры:** Qwen2.5-VL 3B или 7B
  - 3B: быстрее (~100 сек), надёжнее, RAM 68%
  - 7B: детальнее для архитектуры, но RAM на пределе и медленнее (~260 сек)
- **Каталог Пиранези:** https://nasledstvo2026.github.io/Phoenix/piranesi-catalog/ (670 объектов из Рейксмузеума, Rijksmuseum Search API без ключа)
